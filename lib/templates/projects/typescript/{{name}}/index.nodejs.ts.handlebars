/**
 * This file contains imports that are 
 * valid and executable in the browser and 
 * in nodejs.
 */

import * as types from "./types/interfaces"
import { DESCRIPTION } from "./src/{{name}}.package"

export {
    types
}
