import {{project.name}}.{{name}}Function as {{name}}FunctionFile

providedServices.append(
    ProvidedService(
        {{name}}FunctionFile.{{name}},
        ServiceOptions(
            schema={{name}}FunctionFile.SCHEMA,
            id="{{name}}"
        )
    )
)